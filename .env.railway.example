# =============================================================================
# Variables de Entorno para Producción (Railway)
# =============================================================================
# Copia este archivo a .env.production y configura los valores

# === Base de Datos (Railway te da esto automáticamente) ===
# DATABASE_URL=postgresql://...  # Railway lo inyecta automático
POSTGRES_HOST=${PGHOST}
POSTGRES_PORT=${PGPORT}
POSTGRES_USER=${PGUSER}
POSTGRES_PASSWORD=${PGPASSWORD}
POSTGRES_DB=${PGDATABASE}

# === Azure AD (Microsoft Graph API para emails) ===
AZURE_CLIENT_ID=tu-client-id
AZURE_TENANT_ID=tu-tenant-id
AZURE_CLIENT_SECRET=tu-client-secret

# === Correos ===
USER_EMAIL=tu-email@outlook.com
MOM_EMAIL=otro-email@outlook.com

# === Anthropic Claude ===
ANTHROPIC_API_KEY=sk-ant-...

# === JWT Authentication ===
JWT_SECRET_KEY=cambia-esto-a-una-clave-secreta-de-32-caracteres-minimo
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440

# === App Config ===
ENVIRONMENT=production
LOG_LEVEL=INFO

# === Embeddings (opcional) ===
# VOYAGE_API_KEY=...
# OPENAI_API_KEY=...
